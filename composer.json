{
    "name": "globaldelivered/logistics",
    "description": "Global Delivered Logistics - Enterprise Courier Management System",
    "type": "project",
    "require": {
        "php": ">=8.1",
        "phpmailer/phpmailer": "^6.9"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        },
        "files": [
            "app/Helpers/helpers.php",
            "config/constants.php"
        ]
    },
    "scripts": {
        "post-install-cmd": [
            "echo 'GDL installation complete! Run the database schema from database/schema.sql'"
        ]
    },
    "config": {
        "optimize-autoloader": true
    }
}
